# MFR.0.2R-H0.1 — Structural Decision-Pivot Sieve

Authority: [exact researcher request](MFR_0_2R_H01_RESEARCHER_SOURCE.txt).
Required start: `64250a992fb4fa1035a0b1687df7eef326dcb40c`.
Q1.6R and all preceding analytical cores remain frozen (564 pinned files).

## Scope

Rebuild the structural human-review universe from the frozen 273 qualified Job
relations. Preserve every relation, target, provenance path and unresolved evidence
record. No qualification, acceptance, canonical hierarchy or new human judgment.
Historical H0 and diagnostic references enter only after generic outputs freeze.

## Structural contracts

Reuse the exact Q1.3 AssignmentModel feasibility oracle. Only established hard
contracts apply: one mother, strict/equal-level consistency and acyclicity after
parallel contraction, explicit provenance-backed SB12 conflicts and exclusions.
No distance, shared canonical mother, level assignment or historical judgment rule.

Enumerate all minimal forbidden sets by conflict-directed deletion. A different
minimal core must omit at least one member of a found core. Coherent pools terminate;
there is no Cartesian product of undecided edges. Build decision components only
from minimal conflicts. Each review item requires two coherent positive assignments
with incompatible union, retaining their shared positive context and exact constraints.
These assignments are feasibility witnesses, not chosen or exhaustive global variants.
Global component membership does not itself make a local target pivot: that additionally
requires positively distinct commitments for the same target in a minimal conflict.

Every qualified relation has exactly one disposition; every Job target has a status.
For noncompeting qualified targets with unresolved evidence, primary target_status is
EVIDENCE_GAP_NOT_STRUCTURAL_DECISION (researcher S8); structural_status retains the
single-candidate/compatible-set classification. Targets with no qualified relation
remain NO_QUALIFIED_RELATION. All gaps remain in the separate register. A gap cannot
create an edge, an exclusion, or a human decision. Unknown canonical level is not
itself incompatibility under the current hard contracts.

## Review and evidence

Packets preserve all alternatives and all exact witness/raw IDs using Q1.6R roles.
Grammar rules, native features, pre-relation, reference, configuration, lexical-form,
shared-raw and global-constraint evidence remain separately inspectable. All canonical
human review fields are imported; only UNREVIEWED is prefilled. Nondecision groups
retain every relation ID; their deterministic minimum-ID representative is QA only.

Required numbered outputs 01–19, metadata 90 and manifest 99 follow the request.
Supplemental machine-readable evidence catalogs, frozen outcomes, historical and
post-freeze receipts make compact presentation traceable. Empty review sets are valid.

## Validation and authorization

Syntax, focused tests, 14 synthetic cases, negative mutations for all 36 gates,
full regression without skips, and independent A/B ZIP/CRC/manifest checks are required.
Researcher authorization includes real frozen-input execution and commit/push after
all gates pass. A concrete compatibility-model defect requires a stop, not a silent
frozen-core fix. READY_FOR_MFR_0_2R_H0_2 does not authorize starting H0.2.

See [execution instructions](README_MILAL_MFR_0_2R_H01.md).
