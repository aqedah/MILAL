# MFR.0.2R-Q1.6R — mechanism ontology review

Authority: [exact researcher request](MFR_0_2R_Q16R_RESEARCHER_SOURCE.txt).
Baseline: `3efc719428a72419c260e12d918cedd84cef298b`.

This additive stage classifies frozen SB01–SB12 processes, not new relations.
Primary scope is JOB. No external corpus analysis or hierarchy adjudication runs.
All 550 prior file pins and three verified input archives are required. The four
local scholarly PDFs remain untracked; two directly consulted sources have exact
hashes/sections in configuration. No frozen analytical core may change.

## Audits

Process roles derive from native ownership, existing positive witness paths,
explicit analytical dependencies, parameter observations and context provenance.
SB05 audits every historical candidate using exact atom ownership. Native clause
membership proves pre-relation placement, not obligatory valency. Time/Loca
reference-form membership does not prove frame anaphora. Such forms retain their
existing reference machinery and unresolved identities. Derived domain views
cannot add an independent binding reason. Lexical form and downstream lexical
semantics have separate explicit subtypes. No human judgments are synthesized.

The typed provenance projection preserves the entire historical closure while
separating core observation, inherited analytical inputs and context. Every raw
ID must be in the historical closure. Pair comparisons preserve all witness pairs
with exact candidate identity; qualified-outcome comparisons are separately
identified. Six independence classes are descriptive, never scores or votes.

## Deliverables and verification

Required numbered outputs 01–18, metadata 90 and SHA256 manifest 99 follow the
researcher request. Additional machine-readable typed inputs, readiness evidence,
visibility audit, Q1.3 compatibility and diagnostic receipts preserve traceability.
Job controls run only after generic audit files are hashed and frozen.

Syntax, focused tests, ten synthetic cases, all 25 negative gate mutations and
full regression with zero skips precede release. Independent A/B executions must
produce identical manifests and ZIP bytes with valid CRCs. No new relation may
qualify. Any discovered missing explicit primary binder or frozen bug is reported
without changing qualification. Readiness is bounded to reviewed frozen evidence
and authorized formal contracts; unresolved projection/classification prevents
READY. H0.1 requires separate authorization even if READY is returned.

See [execution instructions](README_MILAL_MFR_0_2R_Q16R.md). Researcher authorization
includes real frozen-input execution, validation, commit and push after gates pass.

Source-grounded findings: [methodological note](MFR_0_2R_Q16R_METHOD_NOTE.md).
