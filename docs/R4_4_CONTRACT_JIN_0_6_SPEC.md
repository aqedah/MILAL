# JIN.0.6 — strict clause versus macro hierarchy relation typing

Authority: [exact request](R4_4_CONTRACT_JIN_0_6_RESEARCHER_SOURCE.txt).
Baseline: `a3d770425004af8d635518271a3d640aaf8889dc`.

This is a post-discovery, source-grounded typing audit, not an independent linguistic
discovery pass or R4.4 consumer. It consumes the exact JIN.0.5 archive, preserves every
upstream member, and verifies 243 frozen repository files. It adds no accepted relation,
mother edge, human judgment or migration. All proposals and Q-A–Q-F are UNREVIEWED.
Q1–Q9 remain UNAPPROVED; participant arc remains UNADJUDICATED.

## Universe and identity

Inventory the seven disjoint edge tables of HSA3-LAYER.0.1 inside the frozen archive:
textual hierarchy, same-level, composition, transition, overlay, negative constraints
and technical navigation. HSA3-LAYER.0.2 and JIN.0.4 decisions remain separate overlays;
the latter's latest status is joined only through the exact historical relation ID.
No duplicate counting of older graph snapshots or opposite-direction deduplication.
All canonical nodes enter the node crosswalk, including aliases, groups and evidence anchors.
Primary means CHILD_OF, HIERARCHICALLY_ABOVE and CONTINUES_WITHIN. The complete inventory
also includes every secondary/control row. Report both totals with their distinct scopes.

Every inventory row retains its exact source member, hash, CSV data-row number, row hash
and full original record. HSA1/HSA2 rationale joins through source_judgment_ids. Other
stages retain their original structured records. JIN.0.2/0.3/0.4/0.5 evidence is joined
through explicit historical relation and pair IDs, never reference/text similarity.

Source-recorded clause/atom anchor arrays come from the explicit canonical crosswalk.
They describe evidence anchors, not unit coverage or a unique strict syntactic endpoint.
The source_clause_id and target_clause_id fields therefore preserve the complete source
array rather than choosing one ID; missing identity is UNRESOLVED. No anchor count proves
single-clause status. Only explicit accepted span annotations supply unit spans; a
reference_start/reference_end judgment locus is not substituted for unit coverage.
No single-clause/atom extent is inferred. Missing evidence remains explicit.

## Proposal rules

Historical TEXTUAL_HIERARCHY/TEXTUAL_SAME_LEVEL records with textual-unit or human
structural evidence support a macro proposal (RL2), not an accepted strict mother.
RL1 needs independently established local dependency; RL3 requires independent evidence
in both layers. The current macro registry does not establish either merely through
anchoring or a candidate clause-pair hypothesis. RL4 is available for historical claims
that actually mix layers; generic naming alone is flagged as potential overloading,
not asserted to prove mixed usage. RL6 represents unresolved hierarchy classification.

CONTINUES_WITHIN uses RL5 non-parent placement. An explicitly recorded NO_BOUNDARY
function yields NO_NEW_BOUNDARY placement; other textual continuation remains
MACRO_TEXTUAL_CONTINUATION. Preserve source-specific rationale for 28:1→27:1,
42:16→42:7 and 3:2→3:1. These are not declared identical syntactic relations.

SAME_LEVEL_SIBLING and PARALLEL_ENDING are macro parataxis proposals where the source
is textual. All 98 SAME_LEVEL rows remain individually inspectable; no motherless error.
The strict controls are two separately identified Job 32:3 clause-pair controls, not
new registry edges. Composition/transition/overlay/negative/navigation controls keep
their own proposed layers. RL hierarchy categories are inapplicable to these controls;
RL6 counts for them do not mean their already identified non-parent layer is unknown.
DIRECT_LOCAL_CLOSURE remains non-parent; no strict mother is inferred from closure.

Summary partitions are strict (RL1), macro (RL2), mixed (RL3/RL4), unresolved (RL6)
and non-parent placement (RL5). The last is an additional explicit count, so totals
reconcile without calling placement a strict or accepted macro parent.
Potential label overloading counts the four generic families lacking strict/macro
typing; it does not establish that these labels have an attested strict use.

1:13 retains the historical scene-containment rationale, reopened status and JIN.0.5
zero strict mother-support result. 3:1/3:2 retains speech framing, reverse continuation,
and the actual N→Q transition 3:2→3:3. 40:1→38:1 retains the independently reconfirmed
macro judgment and its storm-formula/challenge distribution, with strict mother unproven.
2:11 and 32:1 retain NO_MACRO_MOTHER_FOUND without a new search.

## Future contract, never migration

Output 14 proposes separate relation_type and relation_layer fields. The vocabulary
includes strict hierarchy, macro hierarchy, textual/macro parataxis, composition,
transition, responsio overlay, negative constraints and technical navigation. Unknown
is an audit status, not an adopted schema enum. MACRO_WITHIN, MACRO_CONTAINS,
MACRO_PARALLEL, STRICT_MOTHER_OF and STRICT_DAUGHTER_OF are discussion names only.
CONTINUES_WITHIN requires explicit typed semantics; other families may reuse names
with an explicit layer. Output 15 preserves each historical ID and type separately.

## Execution and validation

Outputs 01–20, 90 and 99 implement the requested contract; 21 records proposed vocabulary
and 22 preserves authority bytes. Every original JIN.0.5 member remains under history/.
Model gates compare coverage, evidence, proposals and human fields against the source-derived
expected records; targeted mutations cover every model and preflight invariant. Manifest
and release checks have corruption/nonidentity/failure/skip negative tests.
Syntax, new unit tests, full regression (zero skips), self-test and visual text inspection
precede real frozen-source execution and an independent deterministic rerun. No new raw
BHSA extraction occurs: this task audits reproducibly frozen BHSA-grounded evidence.
Readiness is READY_FOR_RELATION_LAYER_HUMAN_REVIEW, never R4.4 implementation ready.
