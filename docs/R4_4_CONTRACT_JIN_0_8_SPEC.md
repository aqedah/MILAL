# JIN.0.8 — Revised Contract Human Freeze

Authority: [exact researcher request](R4_4_CONTRACT_JIN_0_8_RESEARCHER_SOURCE.txt).
Baseline: `f3b08ae8e07b6e9a54c9e62569bf1e6219415562`.
Consume only SHA256-pinned JIN.0.7 results. Preserve all 465 upstream members and
263 frozen repository files. No lower-layer code or historical artifact is rewritten.

## Authorized decisions

Exactly ten primary human decisions, R1–R10, are transcribed as ACCEPTED from the
researcher's exact source. Preserve R3/R8 QUALIFICATION and R10 REWORDING separately.
Each record includes exact source byte offsets, excerpt SHA256, source SHA256,
version, day-precision recording date, invariant links and historical question links.
No unknown event time is inferred. These are supplied judgments, not automatically
completed review fields. The historical JIN.0.7 blank R1–R10 records remain untouched.

R1 freezes the canonical LAYERED_TYPED_GRAPH and nine existing relation layers.
R2 restricts the one-mother rule to human-adjudicated STRICT_HYPOTACTIC_DAUGHTER.
R3 explicitly extends that rule to human-adjudicated MACRO_HYPOTACTIC_DAUGHTER,
without requiring mothers for every macro unit. R4 keeps unresolved strict parentage
independent of accepted macro relations. R5/R6 exclude non-textual composition and
technical navigation from textual motherhood. R7 preserves negative assertions as
first-class records, distinct from absent edges. R8 allows append-only overlays;
lower-layer revision requires EXPLICIT_VERSIONED_SUPERSEDING_HUMAN_ADJUDICATION.
R9 requires layer-specific validation. R10 leaves strict/macro root cardinality
to separate empirical audits. No root is selected here.

## Contract, schema and accounting

Freeze source-grounded CINV-R44-001 through CINV-R44-020. They are derived invariant
records, not twenty additional human judgments. relation_type, relation_layer,
semantic_subtype, strict_status and macro_status coexist; all eight status axes in
request §14 remain separate. The conceptual schema includes every field in §13
and the additional status axes. No new BOUNDARY_PLACEMENT layer is introduced.

Carry forward 111 exact-ID JIN.0.7 typing applications as derived contract
interpretation metadata, separately from ten primary decisions. Seven provisional
continuations remain provisional. The full 250-relation preview does not invent a
future layer for the 139 relations without approved JIN.0.7 typing: their future
layer is UNRESOLVED_NOT_ADJUDICATED. All previews are NOT_EXECUTED. Historical IDs,
types, source rows, directions, review states and evidence remain lossless.

Ten fixtures preserve existing IDs and source rows. In particular, F7 preserves
negative assertions at 37:24→38:1 alongside independently existing overlay records;
it does not invent a direct overlay edge between those two anchors. F8 preserves
31:40 closure, composition, transition and negative dimensions separately. 2:11
and 32:1 remain NO_MACRO_MOTHER_FOUND with their other structural facts intact.

Historical Q1–Q9 retain their original artifact fields. A separate crosswalk marks
them SUPERSEDED_FOR_ACTIVE_REVIEW_BY_REVISED_R1_R10. Its still_open=false applies
only to the contract question, never to unresolved empirical hierarchy questions.

## Remaining research scope

The open registry contains three explicit strict questions, two macro-placement
questions, two separate top-level/root questions and seven provisional continuation
questions. It also retains all 206 JIN.0.3 contextual cases as strict applicability/
scope-review questions, with exact subsequent JIN.0.4 batch judgments attached.
Those candidate scope records do not assert 206 new unresolved strict edges and
do not reopen accepted macro/control judgments. Their historical candidate, pair,
context and locus IDs remain in linked source records. The larger JIN.0.2 inventory
and JIN.0.1 applicability audit remain unchanged in history; no indiscriminate
single-mother demand is applied to their nodes.

The resulting 220 records are review-scope records, not a count of unique established
relations or disjoint empirical problems. The case-level questions may overlap the
five named controls. No fuzzy deduplication or silent deletion is used. Canonical
review fields are imported; only UNREVIEWED is prefilled.

The next audits separately consider strict and macro top-level configurations,
including a unique root, top-level parataxis, other supported configurations and
insufficient evidence. The macro plan also allows a higher macro frame. Allowable
evidence and prohibited heuristics follow request §12. Job 1:1, JOB_BOOK and the
requirements of a tree library cannot preselect a root. Participant/reference
continuity in the evidence plan does not authorize identity resolution now.

## Methodological statements

MILAL is a layered typed graph overall, but hierarchy remains an explicitly typed analytical relation rather than a metaphor for all links.

Strict clause hierarchy and macro textual hierarchy are distinct.

Jin’s single-mother principle applies to strict hypotactic daughters.

By explicit methodological extension, a macro unit adjudicated as a macro hypotactic daughter likewise requires exactly one macro mother.

Paratactic units do not receive synthetic mothers merely to satisfy a tree representation.

Top-level/root cardinality is an empirical question for each hierarchy layer and is not pre-assumed.

Non-textual composition, transition, overlay, negative constraint and technical navigation do not substitute for textual mother-daughter relations.

## Execution and release

Run syntax, new tests, full regression with zero skips, synthetic self-test and
output inspection before real frozen-input execution. Validate every gate with a
negative test; compare two independent real ZIPs byte-for-byte. Independently verify
recursive manifests, frozen pins, source excerpts, fixture semantics and historical
row identity. No raw BHSA reload or new analytical extraction is required.

Required outputs 01–13, 90 and 99 follow the request. Additional outputs:
14 derived contract interpretations, 15 machine-readable conceptual contract,
16 exact researcher source. The complete upstream package is under history/jin_0_7/.
Run gates are recorded in 13; deterministic-rerun and regression are external
release gates verified separately. Output generation alone does not claim them.

Successful contract status: REVISED_R4_4_CONTRACT_HUMAN_FROZEN.
Implementation readiness: BLOCKED_PENDING_TOP_LEVEL_AND_REMAINING_HIERARCHY_AUDITS.
Participant arc remains UNADJUDICATED and separate future research. New analytical
relations, parents, composition relations, roots, participant overlays and migrations
are all zero. R4.4 consumer is absent. Commit/push is authorized after all checks pass.
