# JIN.0.7 — human relation-layer adjudication freeze

Authority: [exact researcher request](R4_4_CONTRACT_JIN_0_7_RESEARCHER_SOURCE.txt).
Start baseline: `148d741e63986c2a209ed770e6682fe8925b4e66`.
Consume only the SHA256-pinned JIN.0.6 result ZIP. Preserve its 449 members and all
253 frozen repository files. No historical analytical core, question or graph row changes.

## Human accounting and source

Exactly six primary decisions, Q-A–Q-F, are transcribed from explicit ACCEPTED statements.
Each has a source SHA256, exact byte-range excerpt and excerpt SHA256, version and
day-precision record date. No unknown event timestamp is invented. Configured excerpts
must match the source bytes before any output is generated. These decisions are authorized
researcher input, not computationally filled review fields.

Separate derived application records cover the existing 111 distinct relation IDs:
2 CHILD_OF + 1 HIERARCHICALLY_ABOVE + 10 CONTINUES_WITHIN + 98 SAME_LEVEL_SIBLING.
Q-B and Q-D both apply to the one 3:2→3:1 ID; that row is not duplicated. Every application
has derived_from_human_rule=true and new_human_decision=false. Six primary decisions
must not be reported as 111 or 112 new researcher judgments.

Primary decision records preserve arrays of scoped historical IDs/types. The historical
crosswalk holds singular IDs/types plus exact source rows, hashes and one-based CSV row
numbers. Only interpretation is superseded: the historical relation and its earlier
review status remain exact. This does not execute consumer precedence or graph migration.

## Accepted interpretation

- Q-A: 1:13 CHILD_OF 1:6, MACRO_TEXTUAL_HIERARCHY / macro containment accepted;
  strict motherhood remains UNRESOLVED_UNPROVEN. Prior parataxis evidence is preserved.
- Q-B: 3:1 HIERARCHICALLY_ABOVE 3:2 is a macro speech-event frame; reverse
  CONTINUES_WITHIN is SPEECH_FRAME_CONTINUATION. Neither asserts a strict syntactic mother.
- Q-C: 40:1 CHILD_OF 38:1 remains macro containment, with the researcher-supplied
  storm-formula, repeated-challenge and distributional rationale. Strict mother unproven;
  lower-level pairwise parataxis evidence remains intact.
- Q-D: future continuation distinguishes STRICT_SYNTACTIC_CONTINUATION (independent
  strict evidence required), MACRO_TEXTUAL_CONTINUATION and NO_NEW_BOUNDARY.
  3:2 has the accepted speech-frame subtype. The seven expressly provisional examples
  (1:14/16/17/18, 2:9, 11:4, 1:5) retain PROVISIONAL_CURRENT_AUDIT macro status.
  28:1→27:1 and 42:16→42:7 are accepted NO_NEW_BOUNDARY placement, never parenthood.
- Q-E: 98 SAME_LEVEL rows receive MACRO_TEXTUAL_PARATAXIS typing in this separate
  application overlay. No strict parataxis or mother edge is inferred.
- Q-F: relation_type and relation_layer are separate mandatory analytical fields in
  the approved conceptual future contract. The vocabulary is exactly the nine values
  in request §12; do not carry forward JIN.0.6's shorter proposal synonyms as new enums.
  No BOUNDARY_PLACEMENT addition. semantic_subtype suffices for current NO_NEW_BOUNDARY.

MILAL's overall conceptual model is LAYERED_TYPED_GRAPH. The six I1–I6 distinctions
are approved and recorded. This freeze is not an executable consumer or a migration
of the full 250-relation registry; the 139 unscoped relations are preserved without
new typing application. Strict positive controls remain controls, not new human edges.

## Open contract questions

The exact historical Q1–Q9 wording is recovered from the pinned JIN.0.1 revised packet,
the source of these question IDs. JIN.0.2's RQ1–RQ7 are different IDs and are preserved
in history, not substituted for Q1–Q9. All Q1–Q9 remain unapproved. The crosswalk records
the effect of this approval, open status and suggested revised wording.

R1–R10 remain UNREVIEWED with canonical review fields imported, every answer blank.
The researcher approved the conceptual distinction but explicitly requested another
contract review: R1 concerns canonical operational adoption, and R2–R10 preserve those
unanswered operational/scope questions. Do not auto-answer R1/R2 from the approved core.
2:11 and 32:1 retain NO_MACRO_MOTHER_FOUND without a search or selected parent.
Participant arc remains UNADJUDICATED. No whole-book root is selected.

## Outputs and validation

Outputs 01–11, 90 and 99 follow the request. Extras: 12 approved conceptual contract JSON,
13 exact researcher source, 14 unchanged strict-control records. Full JIN.0.6 is under
history/jin_0_6/. Output filenames do not constitute relation migration.

Syntax, new unit tests, complete regression with zero skips, synthetic self-test and
human-packet inspection precede real frozen-source execution. Run an independent second
process and compare full ZIP bytes. Verify every recursive manifest and all preserved
source bytes; negative tests cover every model/preflight gate and release invariant.
No new BHSA extraction occurs. Windows is the tested execution environment; Termux
commands are supplied without an empirical claim unless actually executed.

Successful readiness: READY_FOR_REVISED_R4_4_CONTRACT_REVIEW, never implementation ready.
Commit/push is explicitly authorized by request §23 only after validation passes.
