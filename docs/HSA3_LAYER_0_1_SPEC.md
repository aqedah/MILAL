# HSA3-LAYER.0.1 — Layered Integration and Parentage-Necessity Audit

Baseline `45a23a833d98c1e6aa7f37824d98ac6c85768fa4`, main.
Authority: [exact researcher request](HSA3_LAYER_0_1_RESEARCHER_SOURCE.txt).
This implements the requested audit, synthetic/real frozen-artifact validation,
independent rerun, and commit/push after PASS. No fresh BHSA extraction, structural
adjudication, participant-frame acceptance, parent inference or R4.4 code.

## Source contract

Consume only `hsa3_fg_final_real_20260923_a_results.zip`, SHA256
`64cf25abd09fca7355abbcb3740cc46ed26f61b71e5d895ee91383c758974798`.
Verify ZIP CRC, all manifests, stage/mode/gates, A–G FROZEN, and lossless historical
parent/triage identities. Pin 152 prior repository files. Preserve all 202 input
members byte-for-byte under `history/hsa3_fg/`. Synthetic mode calls the existing
frozen synthetic builders; it cannot consume an empirical ZIP.

Original triage regression: KNOWN_CONTAINER 19, TRUE_GLOBAL_SEAM 14, LOCAL_RELATION
10, GROUP_PARENTAGE 8, ROLE_ALIAS_OR_SAME_TEXTUAL_LOCUS 6, total 57. These exact
Job controls live in configuration, not classifier semantics. Fail on mismatch.
All original direct_parent values remain UNRESOLVED, regardless of proposal.

## Lossless canonical projection

Preserve the 61 R4.3 node IDs and all original records. Add existing accepted A/C
and F/G composition groups using their exact IDs. Include the existing FG clause
evidence endpoints used by negative constraints as SOURCE_EVIDENCE_ANCHOR, not
new textual boundary nodes. No new structural node or relation is adjudicated.
Attach accepted A/C labels/unit spans, F/G response-unit spans and the D/E
sequence scope as separately sourced annotations on the original node IDs.
Preserve their source rows/JSON pointer without overwriting R4.3 functions.

The original 205 edge IDs, accepted new A/C, D/E and F/G action records, and
accepted ANA relations are projected once per existing canonical ID. Earlier
CONFIRMED_EXISTING rows append provenance to the existing relation, not another
edge. Unknown types, duplicate IDs, mismatched confirmations or unresolved exact
source identities stop execution. Preserve original record, relation dimension,
authority, source stage, source status and frozen status. Distinguish source
HUMAN_ACCEPTED_SOURCE, AUTOMATIC_DERIVED_FROM_HUMAN and AUTOMATIC_TECHNICAL;
the integration itself is always AUTOMATIC_LOSSLESS_PROJECTION, never a judgment.

Every projection has exact source member, one-based raw data row, identity field
and value, row hash, member hash and input artifact hash. Original evidence and
judgment IDs remain in full original_record. Preserve historical reciprocal
textual sibling rows. ANA reference/scope endpoints retain their native fields;
do not invent node equivalence from reference strings or interval containment.

Use a configured, exhaustive relation-type-to-report-layer map:

- TEXTUAL_HIERARCHY file: CHILD_OF, HIERARCHICALLY_ABOVE, CONTINUES_WITHIN,
  DIRECT_LOCAL_CLOSURE. This is a typed placement view, not a claim that all four
  types are direct parents. Only CHILD_OF/HIERARCHICALLY_ABOVE assert hierarchy.
- TEXTUAL_SAME_LEVEL: SAME_LEVEL_SIBLING and PARALLEL_ENDING, preserving both types.
- COMPOSITION_GROUPING: membership, cycle onset, introduction, enclosing-group
  termination and composition peers. No conversion to textual parentage.
- TRANSITION: TRANSITION_COMPONENT, POST_SPEECH_NARRATIVE_TRANSITION and ANA-Q2
  POST_CLOSURE_TRANSITION. Q2 retains its original TRANSITION_OVERLAY dimension.
- OVERLAY_RESPONSIO: accepted ANA-Q4/Q5 only. Deferred Q3 goes to unresolved file.
- NEGATIVE_CONSTRAINT: all exact historical exclusions, with dimensions intact.
- TECHNICAL_NAVIGATION: all technical root links in a separate additional file.

The seven relation files partition the projected relation IDs exactly. The
unresolved/deferred file contains the 57 parent questions plus ANA-Q3. The future
participant-arc scope is a separate research note, not a new accepted question
node, span, structural group or relation.

## Role and group audit

The historical six role rows represent three paired loci, not six aliases.
Require the frozen EXPLICIT_RESEARCHER_PAIR_AND_SHARED_EXACT_EVIDENCE relation,
actual shared evidence and one SPEECH_UNIT_ONSET in each pair. That speech node
is the canonical textual node; the DIALOGUE_CYCLE_ONSET record is a role alias.
Both historical nodes and their different functions/edges remain. Do not merge
them, delete a speech unit, or infer identity from shared reference alone.
P3 proposes avoiding duplicate independent role-parent questions; it does not
settle the canonical speech node's textual parent or remove its placement review.

Identify the eight historical non-textual groups by actual structural_function
NON_TEXTUAL_GROUP, not the expected Job ID list. Export all existing membership
IDs and positions, including child groups. Additional accepted composition groups
are also present in the canonical group table. Parent necessity is a proposal,
not a new human claim. No composition node is converted to textual status.

## Deterministic proposal policy

Each original row gets one final P2–P7 AUDIT_PROPOSAL with proposal_status
UNREVIEWED. P1 records the starting question only, not a positive conclusion that
a parent exists. Import canonical REVIEW_FIELDS; all researcher answers remain
blank except review_status=UNREVIEWED. Exact direct parent stays unknown.

Ordered rules, with no reference/node-ID-based classification:

1. Non-textual group schema → P2.
2. Verified explicit role pair → P3; preserve canonical and alias distinction.
3. Historical local category plus existing typed local/termination support → P4.
4. Historical known-container category plus accepted outgoing membership,
   textual peer relation and SPEECH_UNIT_ONSET function → P5.
5. Historical global category plus frozen seam and independent typed peer,
   local, introduction, terminal or transition support → P6. An explicit
   transition involving an immediate accepted container may support this report
   proposal; it does not create a new edge for its member.
6. Otherwise → P7, a conservative researcher-review remainder.

Membership and a negative exclusion alone do not meet the global sufficiency
rule. This makes friends arrival a plausible review question without hard-coding
2:11. Removing necessary evidence changes the proposed category; tests prove
this for local, container and global cases. No target P7 count is embedded.
P7 is not proof a parent must exist or a mathematically minimal scholarly set.
Researchers may revise any of the 57 proposals, including P2–P6.

For each row export original triage/unresolved records, canonical role crosswalk,
function, ordered group members, memberships, peers, local relations, transitions,
negative constraints, controlling frozen seam, exact evidence links, rationale,
collapse risk and blank canonical review fields. Closure, no-boundary and
continuation remain distinct source relations; none becomes a hidden parent.

## Layered matrix and next-scope notes

The seven requested main components are display rows. Their spans are explicitly
supplied in the current request as display metadata, not new unit coverage claims.
Reporting context traverses only explicit GROUP_MEMBER_OF edges with a visited
set, including nested response complexes. It creates no transitive edge. Overlay
context uses exact judgment evidence IDs or shared BHSA verbal-word witnesses
from the frozen FG-PREP panel and ANA:W namespace, not verse overlap. Shared
witnesses label reporting context only, not canonical endpoint identity. Native
relation endpoints remain unchanged. The machine matrix
retains every context relation ID, typed view, onset/terminal function and
unresolved question. The readable table summarizes relation types/counts and
links back to the full matrix and canonical registries.

FRIENDS_ENTRY_RESOLUTION_PARTICIPANT_ARC stays NEXT_RESEARCH_SCOPE: opening
2:11–13 and resolution 42:7–9 are separate candidate windows. Future questions
include friend-set continuity, adjudication/obedience, disappearance, 42:10
restoration, relation to 32:1–5, possible חרה אף correspondence and narrator voice.
No accepted 2:11–42:9 span, parentage or 42:10 boundary is produced.

R4.4 readiness is a design recommendation only. Repository search found no
implemented R4.4 schema requiring a parent-complete tree. Such a requirement,
if introduced, would conflict with the current model. Recommend a contract
preserving partial textual graph, composition, transition, overlays, negatives,
technical provenance and unresolved states. Lossless consumption without
resolving 57 parents is feasible in principle, not empirically validated R4.4.

## Required methodological statements

“A direct textual parent is not a mandatory property of every MILAL node.”

“Unresolved parentage must be distinguished from parentage that is not required
by the analytical layer.”

“Composition membership, textual hierarchy, transition, and rhetorical overlay
are independent relation dimensions.”

“Therefore the number of unresolved R4.3 parent fields is not itself a measure
of structural incompleteness.”

NO DIRECT TEXTUAL PARENT ≠ STRUCTURAL INFORMATION MISSING.

## Validation and outputs

Emit every requested file, plus technical navigation, role crosswalk, preserved
seam records and exact request. Keep all upstream artifacts nested unchanged.
Validate syntax, stage/full tests without skips, every computed gate with a
negative mutation, self-test and readable synthetic outputs before real execution.
Two independent real invocations must produce byte-identical complete ZIPs.
Validate manifests, raw provenance rows, partition completeness and frozen pins.
No new human judgment, structural relation or composition relation; no R4.4.
