# Inputs

Place real prior-stage result files here when available:

- `job_r3b_2_results.zip`
- `job_r3b_3_results.zip`

Do not commit corpus datasets, credentials, or large local Text-Fabric caches unless intentionally desired.

Final BHSA real-data validation is expected to occur on Termux using the user's existing BHSA 2021 installation.
